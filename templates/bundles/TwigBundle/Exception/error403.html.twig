{% extends 'error.html.twig' %}
{% block body %}
    <h1 class="text-danger">{{ 'http_error.name'|trans({ '%status_code%': 403 }) }}</h1>

    <p class="lead">
        {{ 'http_error_403.description'|trans }}
    </p>
    <p>
        {{ 'http_error_403.suggestion'|trans({ '%url%': path('home') })|raw }}
    </p>
{% endblock %}